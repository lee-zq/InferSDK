#include "cv_server.h"
#include <thread>

int CVServer::process(message* msg){
    // std::thread t1(&InstManager::create_inst, InstMgr, msg->type, msg->param, &msg->inst);
    // t1.join();
    return 0;
}